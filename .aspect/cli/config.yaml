configure:
  languages:
  plugins:
lint:
  aspects:
    - //tools/lint:linters.bzl%ktlint

plugins:
    - name: fix-visibility
      from: github.com/aspect-build/plugin-fix-visibility
      version: v0.1.0
